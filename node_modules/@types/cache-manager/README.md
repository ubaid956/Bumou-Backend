This is a stub types definition for @types/cache-manager (https://github.com/jaredwray/cacheable#readme).

cache-manager provides its own type definitions, so you don't need @types/cache-manager installed!